1.理解函数声明
？关于(*(void(*)()0)()的讨论
声明单个变量
float f,g; 当对其求值时，f,g的类型为浮点数类型
声明函数
float ff(); 表达式ff()求值结果是一个浮点数

声明指针
float *pf; *pf的类型是一个浮点书，pf是一个指向浮点数的指针

函数指针
float (*h)(), *g();  g是返回值为浮点型指针的函数，h是函数指针返回值为浮点类型

float (*h)()是一个返回值为浮点类型的函数指针，因此，
(float(*)())表示“指向返回值为浮点类型的函数的指针”的类型转换符


typedef void (*funcptr)();
(*(funcptr)0)();

